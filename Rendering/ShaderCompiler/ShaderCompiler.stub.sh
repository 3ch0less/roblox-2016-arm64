#!/bin/bash
# ARM64 stub - ShaderCompiler is Windows-only (HLSL->GLSL cross-compiler)
# Pre-compiled GLSL shaders must be provided separately
echo "ShaderCompiler stub: skipping HLSL->GLSL compilation (Windows tool not available on ARM64)"
exit 0
